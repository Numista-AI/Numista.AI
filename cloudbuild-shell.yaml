steps:
  # Build the container image specific for Shell
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/numista-shell', '-f', 'Dockerfile.shell', '.']

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/numista-shell']

  # Deploy container image to Cloud Run (new service: numista-shell)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'numista-shell'
      - '--image'
      - 'gcr.io/$PROJECT_ID/numista-shell'
      - '--region'
      - 'us-west1'
      - '--allow-unauthenticated'

images:
  - 'gcr.io/$PROJECT_ID/numista-shell'

options:
  logging: CLOUD_LOGGING_ONLY
