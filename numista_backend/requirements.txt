# =============================================================================
# requirements.txt — PRODUCTION (Cloud Run / Docker)
# =============================================================================
# Only packages imported by main.py and its direct modules:
#   main.py, tier_gatekeeper.py, stripe_config.py, morgan_knowledge.py,
#   coin_legislation_map.py, vertex_search/coin_search_endpoint.py
#
# Local utility scripts (scrapers, audit tools, etc.) use requirements-dev.txt
# =============================================================================

# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.139.0
uvicorn==0.34.3
# starlette resolved automatically by fastapi>=0.139.0 (requires >=0.46.0, no upper cap → installs 1.0.1+)
pydantic==2.11.5
python-multipart==0.0.20

# ── Google Cloud & AI ─────────────────────────────────────────────────────────
firebase-admin==7.3.0
google-cloud-firestore==2.21.0
google-cloud-storage==3.1.1
google-cloud-documentai==3.14.0
google-cloud-discoveryengine==0.13.6
# google-cloud-aiplatform REMOVED 2026-07-01: fully migrated to google-genai SDK
google-genai==1.71.0
google-auth==2.49.0
google-api-core==2.25.1
googleapis-common-protos==1.74.0

# ── Data & utilities ──────────────────────────────────────────────────────────
pandas==2.2.3
feedparser==6.0.12
requests==2.34.2
httpx==0.28.1
python-dotenv==1.1.0
PyJWT==2.10.1
yfinance>=0.2.62  # allows pip to resolve; 1.4.1 pulls websockets/peewee that conflict in Docker

# ── Payments ──────────────────────────────────────────────────────────────────
stripe==12.2.0

# ── HTML/PDF parsing (used in main.py routes) ─────────────────────────────────
beautifulsoup4==4.13.4
pypdf==6.13.3
Pillow==12.3.0

# ── Web scraping (for Scraper Dashboard) ──────────────────────────────────────
curl_cffi==0.15.0

# ── gRPC transport (required by google-cloud-* libraries) ─────────────────────
grpcio==1.73.0
grpcio-status==1.73.0
proto-plus==1.27.2
protobuf==5.29.6
