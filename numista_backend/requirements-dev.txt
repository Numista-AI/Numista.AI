# =============================================================================
# requirements-dev.txt — LOCAL DEVELOPMENT & UTILITY SCRIPTS
# =============================================================================
# Packages used by local data-pipeline scripts, scrapers, audit tools,
# image sourcing utilities, etc. — NOT installed in the Cloud Run Docker image.
#
# Install with:  pip install -r requirements-dev.txt
#
# Note: Dependabot will monitor this file too, but these alerts are
# categorized as "development" (lower urgency than production alerts).
# =============================================================================

# ── Includes all production dependencies ──────────────────────────────────────
-r requirements.txt

# ── Web scraping ──────────────────────────────────────────────────────────────
beautifulsoup4==4.13.4
botasaurus==4.0.97
botasaurus-humancursor==4.0.83
botasaurus_api==4.0.10
botasaurus_driver==4.0.92
botasaurus_proxy_authentication==1.0.16
botasaurus_requests==4.0.38
curl_cffi==0.15.0
playwright>=1.54.0

# ── Image processing ─────────────────────────────────────────────────────────
opencv-python==4.11.0.90
Pillow==12.3.0

# ── Data science / ML tooling ─────────────────────────────────────────────────
numpy==2.2.6
scikit-learn>=1.6.0
joblib==1.5.0
pyarrow==20.0.0

# ── Excel / office file handling ──────────────────────────────────────────────
openpyxl==3.1.5
xlsxwriter==3.2.3
et_xmlfile==2.0.0

# ── Streamlit (local dashboards only) ─────────────────────────────────────────
streamlit==1.45.0
altair==5.5.0
pydeck==0.9.1
extra-streamlit-components==0.1.81

# ── PDF generation / parsing ──────────────────────────────────────────────────
PyPDF2==3.0.1
reportlab>=4.4.0

# ── Git utilities ─────────────────────────────────────────────────────────────
GitPython==3.1.44
gitdb==4.0.12
smmap==5.0.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.4.1
pytest-asyncio>=0.25.0

# ── Misc data/networking utilities ────────────────────────────────────────────
lxml==5.4.0
multitasking==0.0.12
sgmllib3k==1.0.0
soupsieve==2.7
kagglehub>=0.3.0
kagglesdk>=1.0.0

# ── Async / concurrency ───────────────────────────────────────────────────────
gevent==26.0.0
geventhttpclient==2.4.0

# ── System utilities ──────────────────────────────────────────────────────────
psutil==7.0.0
pystray==0.19.5
watchdog==6.0.0
colorama==0.4.6
rich==14.0.0
tqdm==4.67.1
tenacity==9.1.0
click==8.1.9

# ── Database ──────────────────────────────────────────────────────────────────
peewee==4.0.4

# ── Virtualisation (for local Playwright headless) ────────────────────────────
PyVirtualDisplay==3.0

# ── Coin-specific / eBay / market data ───────────────────────────────────────
ebaysdk>=2.2.0
