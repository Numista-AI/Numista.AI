
# .streamlit/secrets.toml
# NOTE: Do NOT store real Stripe keys here. Use numista_backend/.env (gitignored)
# or inject via Cloud Run secrets manager at runtime.
STRIPE_PUBLISHABLE_KEY = "pk_test_REPLACE_ME"
STRIPE_SECRET_KEY = "sk_test_REPLACE_ME"